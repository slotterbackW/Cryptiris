const initialState = {
	loading: false,
	user: null,
	error: null,
}

function currencies(state = initialState, action) {
	switch (action.type) {
		default:
			return state
	}
}

export default currencies